# LLM Tuning Profiles

This folder only ships operator-facing JSON templates that are safe to publish
with the public companion tooling.

The current public focus remains the projected-feed Colorama scene. A retained
controller-breathing template is kept here for tooling compatibility with the
existing profile editors and tests, but it does not change the public repo's
single-runtime posture.

